ion-view.page-home(view-title='home')
    ion-content
        .row
            .col-4
                .card
                    ion-list
                        ion-item(
                            ng-click="ctrl.addPage('test')"
                        ) Add Page
            .col-8
                iframe(
                    src='http://localhost:3000'
                    border='none'
                )